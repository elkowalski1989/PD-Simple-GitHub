<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wpfEngine="clr-namespace:CircuitHub.AllegroBridge.Wpf.Engine;assembly=CircuitHub.AllegroBridge.Wpf"
             x:Class="PD.Simple.Engine.EngineExplorerView"
             Background="#F5F8FD">
  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="12"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <DockPanel>
      <StackPanel>
        <TextBlock Text="Board Explorer" FontSize="27" FontWeight="SemiBold"/>
        <TextBlock Text="Engine-owned live and offline board data. Reads are explicit; selecting rows never performs hidden native I/O."
                   Foreground="#59708E" Margin="0,6,0,0"/>
      </StackPanel>
      <WrapPanel DockPanel.Dock="Right" HorizontalAlignment="Right">
        <Button x:Name="AcquireMetadataButton" Content="Read metadata" Click="AcquireMetadata_Click"/>
        <Button x:Name="AcquireBoardButton" Content="Capture board" Click="AcquireBoard_Click"/>
        <Button Content="Open scene" Click="OpenScene_Click"/>
        <Button x:Name="SaveSceneButton" Content="Save scene" Click="SaveScene_Click"/>
      </WrapPanel>
    </DockPanel>

    <Border Grid.Row="1" Background="#EAF4FF" BorderBrush="#C7DFFB" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,14,0,0">
      <TextBlock x:Name="StatusText" Text="Acquire a live board or open a saved .allegroscene file." Foreground="#45627F"/>
    </Border>

    <Grid Grid.Row="3">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="260"/>
        <ColumnDefinition Width="8"/>
        <ColumnDefinition Width="330"/>
        <ColumnDefinition Width="8"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Border Background="White" BorderBrush="#DCE7F5" BorderThickness="1" CornerRadius="6" Padding="12">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <ComboBox x:Name="Family" SelectionChanged="Family_Changed" SelectedIndex="0">
            <ComboBoxItem Content="Components" Tag="components"/>
            <ComboBoxItem Content="Nets" Tag="nets"/>
            <ComboBoxItem Content="Differential pairs" Tag="pairs"/>
            <ComboBoxItem Content="Layers" Tag="layers"/>
            <ComboBoxItem Content="Pins" Tag="pins"/>
          </ComboBox>
          <TextBox Grid.Row="1" x:Name="Search" Margin="0,8,0,0" Padding="8" TextChanged="Search_Changed" ToolTip="Filter captured objects locally"/>
          <TextBlock Grid.Row="2" x:Name="CoverageText" Foreground="#59708E" FontSize="11" Margin="0,8,0,8"/>
          <ListBox Grid.Row="3" x:Name="Objects" DisplayMemberPath="Name" SelectionChanged="Object_Changed"/>
        </Grid>
      </Border>

      <Border Grid.Column="2" Background="White" BorderBrush="#DCE7F5" BorderThickness="1" CornerRadius="6" Padding="12">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <WrapPanel>
            <Button x:Name="HighlightButton" Content="Highlight" Click="Highlight_Click"/>
            <Button x:Name="ZoomButton" Content="Zoom" Click="Zoom_Click"/>
          </WrapPanel>
          <TextBlock Grid.Row="1" Text="Captured object / relationships" FontWeight="SemiBold" Margin="0,10,0,7"/>
          <TextBox Grid.Row="2" x:Name="Details" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                   VerticalScrollBarVisibility="Auto" BorderThickness="0" Background="#F8FAFD" Padding="8"/>
        </Grid>
      </Border>

      <Border Grid.Column="4" Background="White" BorderBrush="#DCE7F5" BorderThickness="1" CornerRadius="6" Padding="6">
        <wpfEngine:DesignSceneView x:Name="SceneView"/>
      </Border>
    </Grid>
  </Grid>
</UserControl>
